hypermotion
----------
Single Header C Character animation system using an animation graph.
It's composed of 3 stages:
* edit - add nodes, animations and state machines to the graph.
* cook - compile graph into a binary representation
* play - single header C runtime to load graph and animate character graph.
